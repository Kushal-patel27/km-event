# K&M Events - Environment Configuration Template
# Copy this file to .env and fill in your values

# ============================================
# DATABASE
# ============================================
MONGO_URI=mongodb://localhost:27017/km-events
# Production: mongodb+srv://username:password@cluster.mongodb.net/km-events

# ============================================
# REDIS (High-Performance QR System)
# ============================================
REDIS_URL=redis://localhost:6379
# Production: redis://your-redis-server:6379
# With authentication: redis://:password@host:6379

# ============================================
# AUTHENTICATION
# ============================================
JWT_SECRET=your-super-secret-jwt-key-change-in-production
SESSION_SECRET=your-session-secret-change-in-production

# ============================================
# QR CODE SECURITY (High-Performance System)
# ============================================
# Generate a secure 32-byte hex key using:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
QR_ENCRYPTION_KEY=your-32-byte-hex-encryption-key-here

# ============================================
# SERVER
# ============================================
PORT=5000
NODE_ENV=development
# Production: NODE_ENV=production

FRONTEND_URL=http://localhost:5173
# Production: https://your-domain.com

# ============================================
# EMAIL (SMTP)
# ============================================
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
# For Gmail: Generate app password at https://myaccount.google.com/apppasswords

# ============================================
# GOOGLE OAUTH (Optional)
# ============================================
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback
# Production: https://your-domain.com/api/auth/google/callback

# ============================================
# RAZORPAY PAYMENT GATEWAY
# ============================================
RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-key-secret
# Get your keys from: https://dashboard.razorpay.com/app/keys
# Test Mode: Use test keys (starts with rzp_test_)
# Live Mode: Use live keys (starts with rzp_live_)

# ============================================
# HIGH-PERFORMANCE SCANNING (Optional Tuning)
# ============================================
# Rate limits can be adjusted in middleware/rateLimitMiddleware.js
# Redis memory limit: configured in redis.conf
# MongoDB connection pool: default 10 connections

# ============================================
# NOTES
# ============================================
# 1. Never commit .env file to version control
# 2. Use strong, unique values for secrets
# 3. Redis is required for high-performance QR scanning
# 4. For production, use managed Redis (AWS ElastiCache, Redis Cloud, etc.)
# 5. Enable MongoDB replica set for transaction support
